# TaskController

TaskController is the host-neutral controller runtime for DW-SuperApps. Core contracts are independent of controller host, executor runtime, task backend, and visibility surface. GPT is the first pilot host profile; Hermes Cloud is a pilot executor target, not a core dependency.
