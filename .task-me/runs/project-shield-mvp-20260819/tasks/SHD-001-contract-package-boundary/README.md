# SHD-001 — Contract & package boundary
Freeze the SDD and determine the exact Shield module/test/config roots by inspecting current DW-SuperApps conventions. This is the only task allowed to resolve the currently-unverified new package path. Downstream tasks must consume its recorded decision.

**Exit:** stable SDD IDs + explicit included/excluded package boundary; no implementation authority implied.
