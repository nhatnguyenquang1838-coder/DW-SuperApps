# Decisions
- Preserve a separate Shield semantic layer; do not fold Finding/Protection domain into TaskController kernel by default.
- Final new package root is intentionally a SHD-001 decision because no Shield runtime path exists at the bound head.
- Planning artifacts do not authorize implementation.
