# SHD-005 — TaskController workflow adapter
Shield is a durable logical supervisor; material work is a bounded TaskController Run. Reuse existing continuation, A2A, execution and audit seams. A schedule signal starts work but never replaces an active wait/readback loop.
