# SHD-006 — Protection & authority
Implement reversible protection as a separate object/workflow. Local Project Shield evaluates every Global directive. The adapter may validate GWC evidence but must never treat configuration, A2A or human-plane projection as authority by itself.
