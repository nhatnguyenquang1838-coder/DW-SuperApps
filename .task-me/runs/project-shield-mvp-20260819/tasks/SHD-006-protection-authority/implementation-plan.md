1. Write directive authentication/replay/audience/expiry/ACK negative tests.
2. Implement local ProtectionManager + policy evaluator and assurance classification.
3. Implement authority adapter contract referencing exact external scope/approval/readback evidence.
4. Add apply/renew/release/readback flows and PARTIAL/UNKNOWN coverage states.
