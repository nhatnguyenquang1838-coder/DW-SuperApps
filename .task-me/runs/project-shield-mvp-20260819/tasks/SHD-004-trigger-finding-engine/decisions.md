# Decisions
Only validated Finding enters canonical store. Evidence conflict is not resolved by vote. Issue systems are downstream projections. Semantic dedupe may suggest but not silently collapse uncertain findings.
